#include <stdio.h>
#include "bst.h"

int main() {
    // Run automated tests first
    bst_test();

    // Optionally keep the ad-hoc test for manual verification
    // bst_adhoc_test();

    return 0;
}